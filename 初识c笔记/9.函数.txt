C语言是【结构化】的程序设计语言
 - 顺序结构
 - 选择结构
 - 循环结构



//求2个任意整数和

//函数

int Add(int x, int y)     Add函数名     (int x,int y)函数参数    int返回类型
{
	int z = 0;
	z = x + y;   函数体    简化版直接返回return (x + y);
	return z;
}

#include <stdio.h>
int main()
{
	int a = 0;
	int b = 0;
	//输入
	scanf("%d %d", &a,&b);
	//求和

	//int sum = a + b;
	int sum = Add(a, b);

	int t1 = 100;
	int t2 = 300;

	int ret=Add(t1, t2);


	//打印
	printf("sum=%d\n", sum);

	return 0;
}


